<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

        <item android:id="@+id/home"
        android:icon="@drawable/ic_home_black_24dp"
        android:title="Home"/>
        <item android:title="Menu">
            <menu>
                <item android:id="@+id/courses"
                    android:icon="@drawable/ic_course"
                    android:title="Courses"/>
                <item android:id="@+id/assignments"
                    android:icon="@drawable/ic_assignment"
                    android:title="Assignments"/>
                <item android:id="@+id/checkpoints"
                    android:icon="@drawable/ic_checkpoint"
                    android:title="Checkpoints"/>
            </menu>
        </item>
</menu>